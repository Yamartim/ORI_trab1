#include <iostream>
#include <string>
#include <fstream>
#include <iomanip>
#include <stdlib.h>
#include <locale>
#include <limits>

#include "Arquivo.h"
#include "Registro.h"

int main(){

    std::fstream arq;
    int tam;
    arq.open("arquivo2.dat", std::ios_base::in | std::ios_base::out | std::ios_base::binary);

    arq.read((char*)&tam, sizeof(int));

    std::cout << tam << std::endl;

    arq.close();

    return 0;
}